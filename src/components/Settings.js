import React, { useState } from "react";
import TimeField from "react-simple-timefield";

const Settings = () => {
  return (
    <div>
      <h1> would you like to change your session duration? </h1>
    </div>
  );
};

export default Settings;
